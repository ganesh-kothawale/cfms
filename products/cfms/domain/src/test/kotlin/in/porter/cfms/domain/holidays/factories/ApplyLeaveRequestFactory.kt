/*
import `in`.porter.cfms.domain.holidays.usecases.CourierApplyLeaveCallingService.ApplyLeaveRequest

object ApplyLeaveRequestFactory {
    fun build(
        identificationCode: String = "ABC12",
        holidays: List<String> = listOf("2024-12-20"),
        type: String = "Normal",
        backupFranchises: List<String> = listOf("SME001"),
        status: String = "Approved"
    ): ApplyLeaveRequest {
        return ApplyLeaveRequest(
            identification_code = identificationCode,
            holidays = holidays,
            type = type,
            backup_franchises = backupFranchises,
            status = status
        )
    }
}*/
