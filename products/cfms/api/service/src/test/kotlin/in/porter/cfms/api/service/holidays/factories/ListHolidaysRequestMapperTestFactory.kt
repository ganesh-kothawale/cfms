/*
package `in`.porter.cfms.api.service.holidays.factories

import `in`.porter.cfms.api.models.holidays.ListHolidaysRequest
import java.time.LocalDate

object ListHolidaysRequestMapperTestFactory {
    fun createRequest(
        page: Int = 1,
        size: Int = 10,
        franchiseId: String? = "franchise-123",
        leaveType: String? = "Normal",
        startDate: LocalDate? = LocalDate.of(2024, 1, 1),
        endDate: LocalDate? = LocalDate.of(2024, 12, 31)
    ): ListHolidaysRequest {
        return ListHolidaysRequest(
            page = page,
            size = size,
            franchiseId = franchiseId,
            leaveType = leaveType,
            startDate = startDate,
            endDate = endDate
        )
    }
}*/
